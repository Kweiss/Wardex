# Changelog

All notable changes to this repository are documented in this file.

## 2026-02-13

### @wardexai/mcp-server 0.1.1

- Changed default `WARDEX_MODE` fallback from `adaptive` to `guardian`.
- Updated MCP server docs and API reference examples to reflect conservative defaults.
- Added shared defaults documentation under `defaults/` for fast setup.

### @wardexai/claude-skill 0.1.1

- Changed hook runtime default `WARDEX_MODE` fallback from `adaptive` to `guardian`.
- Updated hook script documentation comments to match runtime default.
- Updated Claude skill guide to point to shared defaults setup (`defaults/`).
